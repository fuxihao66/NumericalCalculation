
\section{算法设计：}
\subsection{解方程}
1. 通过表中的每一个(u_i,t_i)数据对，带到方程组中，得到一个四元方程组，把v, w, x, y以外的移到等式右边，方程组如下：

2. 通过观察，发现方程组中的x和y是一次函数，可以通过化简得到一个等价的二元方程组；
3. 使用离散型牛顿法解方程组（细节：初值。。。），得到(v_i,w_i)
4. 把(v,w)代入原方程组，得到(x_i,y_i)
5. 对照表，就得到了((x_i,y_i),z_i)数对
6. 重复以上操作，直到把(u_i,t_i)的36种组合全部遍历，得到了36个函数z=f(x,y)的坐标点，用于下一阶段操作；
\subsection{}